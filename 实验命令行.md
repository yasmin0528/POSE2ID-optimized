# 实验命令行

本文档对应当前仓库中已经落地的 `PQ-IPG / ISAF / QG-ISAF` 推理代码。

## 已落地的改动

1. `PQ-IPG + ISAF` 现在支持真正联合，不再是二选一。
2. 新增 `PQ_TOPK`，可先按姿态质量保留前 `k` 个 pose，再做均值融合、PQ 融合或 ISAF 融合。
3. 最终融合系数 `eta` 不再硬编码为 `2`，改为命令行可调的 `TEST.IPG_FUSION_ETA`。
4. 新增 `TEST.IPG_BRANCH_NORM`，可在最终相加前先对原图分支和 IPG 分支做 L2 归一化。

## 新增配置项

位于 `demo/TransReID-main/config/defaults.py`：

- `TEST.PQ_TOPK`
- `TEST.ISAF_USE_PQ`
- `TEST.ISAF_PQ_BLEND`
- `TEST.IPG_FUSION_ETA`
- `TEST.IPG_BRANCH_NORM`

## 运行前提

1. 已经生成 `query_gen/pose{1..8}/` 与 `bounding_box_test_gen/pose{1..8}/`
2. 已准备好 ReID 预训练权重
3. 以下命令默认在项目根目录执行

## 通用模板

```bash
cd demo/TransReID-main

python test.py \
  --config_file configs/Market/vit_transreid_stride.yml \
  TEST.IPG True \
  TEST.NFC True \
  TEST.PQ_IPG False \
  TEST.ISAF False
```

## 1. Pose2ID 原始基线

等权融合全部 8 个 pose，不使用 PQ，不使用 ISAF。

```bash
cd demo/TransReID-main

python test.py \
  --config_file configs/Market/vit_transreid_stride.yml \
  TEST.IPG True \
  TEST.NFC True \
  TEST.PQ_IPG False \
  TEST.ISAF False \
  TEST.PQ_TOPK 0 \
  TEST.IPG_FUSION_ETA 2.0 \
  TEST.IPG_BRANCH_NORM False
```

## 2. PQ-IPG

使用静态姿态质量权重。

```bash
cd demo/TransReID-main

python test.py \
  --config_file configs/Market/vit_transreid_stride.yml \
  TEST.IPG True \
  TEST.NFC True \
  TEST.PQ_IPG True \
  TEST.ISAF False \
  TEST.PQ_ALPHA 0.4 \
  TEST.PQ_BETA 0.3 \
  TEST.PQ_GAMMA 0.3 \
  TEST.PQ_TEMPERATURE 0.5 \
  TEST.PQ_TOPK 0 \
  TEST.IPG_FUSION_ETA 2.0
```

## 3. ISAF

使用身份相似度动态加权。

```bash
cd demo/TransReID-main

python test.py \
  --config_file configs/Market/vit_transreid_stride.yml \
  TEST.IPG True \
  TEST.NFC True \
  TEST.PQ_IPG False \
  TEST.ISAF True \
  TEST.ISAF_TAU 0.07 \
  TEST.ISAF_USE_PQ False \
  TEST.PQ_TOPK 0 \
  TEST.IPG_FUSION_ETA 2.0
```

## 4. QG-ISAF 推荐版

先使用 `PQ_TOPK` 过滤低质量 pose，再用 `ISAF` 做动态融合，并引入少量 PQ 先验。

推荐先从下面这一组开始：

```bash
cd demo/TransReID-main

python test.py \
  --config_file configs/Market/vit_transreid_stride.yml \
  TEST.IPG True \
  TEST.NFC True \
  TEST.PQ_IPG False \
  TEST.ISAF True \
  TEST.ISAF_TAU 0.07 \
  TEST.ISAF_USE_PQ True \
  TEST.ISAF_PQ_BLEND 0.30 \
  TEST.PQ_TOPK 4 \
  TEST.PQ_ALPHA 0.4 \
  TEST.PQ_BETA 0.3 \
  TEST.PQ_GAMMA 0.3 \
  TEST.PQ_TEMPERATURE 0.5 \
  TEST.IPG_FUSION_ETA 2.0 \
  TEST.IPG_BRANCH_NORM False
```

## 5. 轻量复杂度版本

如果你想把“降低复杂度”作为课程创新点重点，可直接减少生成 pose 数量参与融合。

### Top-3 均值融合

```bash
cd demo/TransReID-main

python test.py \
  --config_file configs/Market/vit_transreid_stride.yml \
  TEST.IPG True \
  TEST.NFC True \
  TEST.PQ_IPG False \
  TEST.ISAF False \
  TEST.PQ_TOPK 3 \
  TEST.IPG_FUSION_ETA 2.0
```

### Top-4 QG-ISAF

```bash
cd demo/TransReID-main

python test.py \
  --config_file configs/Market/vit_transreid_stride.yml \
  TEST.IPG True \
  TEST.NFC True \
  TEST.PQ_IPG False \
  TEST.ISAF True \
  TEST.ISAF_TAU 0.07 \
  TEST.ISAF_USE_PQ True \
  TEST.ISAF_PQ_BLEND 0.30 \
  TEST.PQ_TOPK 4 \
  TEST.IPG_FUSION_ETA 2.0
```

## 6. 关键消融建议

### 6.1 `tau` 消融

```bash
TEST.ISAF_TAU 0.03
TEST.ISAF_TAU 0.05
TEST.ISAF_TAU 0.07
TEST.ISAF_TAU 0.10
```

### 6.2 `PQ_TOPK` 消融

```bash
TEST.PQ_TOPK 0
TEST.PQ_TOPK 3
TEST.PQ_TOPK 4
TEST.PQ_TOPK 5
```

### 6.3 `ISAF_PQ_BLEND` 消融

```bash
TEST.ISAF_USE_PQ True
TEST.ISAF_PQ_BLEND 0.10
TEST.ISAF_PQ_BLEND 0.20
TEST.ISAF_PQ_BLEND 0.30
TEST.ISAF_PQ_BLEND 0.40
```

### 6.4 `eta` 消融

```bash
TEST.IPG_FUSION_ETA 1.0
TEST.IPG_FUSION_ETA 1.5
TEST.IPG_FUSION_ETA 2.0
TEST.IPG_FUSION_ETA 2.5
```

### 6.5 分支归一化消融

```bash
TEST.IPG_BRANCH_NORM False
TEST.IPG_BRANCH_NORM True
```

## 7. 推荐实验表

建议至少记录以下几组：

1. Pose2ID Baseline
2. Pose2ID + PQ-IPG
3. Pose2ID + ISAF
4. Pose2ID + Top-4 Mean
5. Pose2ID + QG-ISAF
6. QG-ISAF `tau` 消融
7. QG-ISAF `eta` 消融

## 8. 当前最推荐的主结果命令

如果你只想先跑一组最有希望的结果，优先跑这个：

```bash
cd demo/TransReID-main

python test.py \
  --config_file configs/Market/vit_transreid_stride.yml \
  TEST.IPG True \
  TEST.NFC True \
  TEST.PQ_IPG False \
  TEST.ISAF True \
  TEST.ISAF_TAU 0.07 \
  TEST.ISAF_USE_PQ True \
  TEST.ISAF_PQ_BLEND 0.30 \
  TEST.PQ_TOPK 4 \
  TEST.IPG_FUSION_ETA 2.0 \
  TEST.IPG_BRANCH_NORM False
```
